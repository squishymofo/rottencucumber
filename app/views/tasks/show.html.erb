Stuff about task

<% if @error_comment %>
  <%= render :partial => 'shared/error_messages', :locals => {:target => @error_comment } %>
<% end %>
<%= form_for @task, :url => new_comment_path(:id => @task.id) do %>
  <%= text_field_tag :comment_body, nil, :title => 'Comment' %>
  <%= submit_tag "Comment!" %>
<% end %>

Comments: 

<% if @comments %>
  <% for comment in @comments do %>
    <%= render :partial => 'shared/one_comment', :locals => {:comment => comment } %>
  <% end %>
<% else %>
  There aren't any commnets yet
<% end %>
