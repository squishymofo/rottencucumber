Group name: 
<%= @group.name %>
<br />

Belongs to organization: 
<%= @group.organization.name %>
<br />

Members: 
<% usrs = @group.users %>
<% usrs.each do |usr| %>
	<%= usr.first_name + " " + usr.last_name + " (" + usr.email + "), " %>
<% end %>
